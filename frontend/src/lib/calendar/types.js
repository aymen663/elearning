/**
 * Calendar module type definitions (JSDoc style for plain JS).
 * 
 * EventType:    "course" | "task" | "exam" | "deadline"
 * TaskPriority: "low" | "medium" | "high"
 * NotificationChannel: "email" | "in_app"
 * NotificationStatus:  "pending" | "sent" | "failed"
 */

// This file exists purely for documentation reference.
// All types are inferred at runtime in the JSX components.
export {};
